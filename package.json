{
  "name": "roproxy",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "start": "bun run server.ts",
    "build": "bun build server.ts --outdir dist --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}